class AppConstants {
  AppConstants._();

  static const defaultPageSize = 20;
  static const maxAttachmentSizeMb = 50;
  static const thumbnailSize = 256;
  static const websocketHeartbeatInterval = Duration(seconds: 25);
}
